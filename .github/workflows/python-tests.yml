name: Kør pytest automatisk

on:
  push:
    branches: [ "fix-tests" ]
  pull_request:
    branches: [ "fix-tests" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Hent kode
      uses: actions/checkout@v4

    - name: Installer Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Installer afhængigheder
      run: pip install pytest

    - name: Kør security gate tests
      run: pytest -v -c /dev/null test/test_examples.py