name: Kør pytest automatisk

on:
  push:
    branches: [ "fix-test" ]
  pull_request:
    branches: [ "fix-test" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Hent kode
      uses: actions/checkout@v4

    - name: Installer Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Installer afhængigheder
      run: pip install pytest

    - name: Kør KUN test_examples.py
      run: pytest -v -c /dev/null test/test_examples.py